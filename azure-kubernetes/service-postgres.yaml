apiVersion: v1
kind: Service
metadata:
  name: edu-service-postgres
spec:
  selector:
    app: edu-service-postgres
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5432
  type: NodePort
  