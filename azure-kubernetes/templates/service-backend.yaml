apiVersion: v1
kind: Service
metadata:
  name: "{{ .Values.app.name }}-{{ .Values.services.backend }}"
spec:
  selector:
    app: "{{ .Values.app.name }}-{{ .Values.services.backend }}"
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
  type: NodePort
  